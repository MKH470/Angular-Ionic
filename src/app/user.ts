export interface User {
  // eslint-disable-next-line @typescript-eslint/ban-types
  name: String;
  // eslint-disable-next-line @typescript-eslint/ban-types
  username: String;
  email: string;
  adress: string;
  street: string;
  suite: any;
  city: string;
  zipcode: any;
  lat: number;
  lng: number;
  phone: number;
  website: string;
}
